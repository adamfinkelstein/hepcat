# example .env file
# copy this file to ../.env and fill out the values as needed

# the configuration to use, eg production or development
FLASK_CONFIG=development

# must provide these secret keys and passwords
SECRET_KEY=<missing>
HEPCAT_ADMIN_PASSWD=<missing>
HEPCAT_CHAIR_PASSWD=<missing>
HEPCAT_SCREEN_PASSWD=<missing>
MAIL_PASSWORD=<missing>

# set to True if the server should allow cross-origin requests.
# useful for serving on localhost with npm during development.
# ALLOW_CORS=

# set to True if you want app to show logs on console
# HEPCAT_SHOW_LOGS=
